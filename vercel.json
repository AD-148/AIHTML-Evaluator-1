{
    "devCommand": "npm run dev",
    "buildCommand": "npm run build",
    "installCommand": "export PLAYWRIGHT_BROWSERS_PATH=0 && pip install -r requirements.txt && python -m playwright install chromium",
    "outputDirectory": "frontend/dist",
    "env": {
        "PLAYWRIGHT_BROWSERS_PATH": "0"
    },
    "rewrites": [
        {
            "source": "/evaluate",
            "destination": "/api/index.py"
        },
        {
            "source": "/(.*)",
            "destination": "/frontend/dist/$1"
        }
    ],
    "headers": [
        {
            "source": "/(.*)",
            "headers": [
                {
                    "key": "Access-Control-Allow-Origin",
                    "value": "*"
                }
            ]
        }
    ],
    "functions": {
        "api/*.py": {
            "maxDuration": 60
        }
    }
}